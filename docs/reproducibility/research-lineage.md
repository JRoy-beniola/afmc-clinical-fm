# AFMC Research Lineage Through Post-Phase-0.6

## Frozen sequence

```text
Phase 0
  -> Phase 0.5
  -> Phase 0.6
  -> D4-B AMBIGUOUS -> STOP
```

That terminal Phase 0.6 route is frozen. Reproducibility work does not reopen it.

## Phase 0

The intended extreme-low-N superiority claim was not validated. The Phase 0 archive was preserved as a valid negative/mixed methodological result and motivated the focused Phase 0.5 mechanistic redesign.

Frozen registry classification:

```text
PHASE 0 COMPLETE — EXTREME-LOW-N HYPOTHESIS NOT VALIDATED.
```

## Phase 0.5

The mechanistic validation program terminated at Stage I-A because the flow mechanism gate was not established.

Frozen registry classification:

```text
PHASE-0.5 TERMINATED AT STAGE I-A — FLOW MECHANISM GATE NOT ESTABLISHED
```

## Phase 0.6

Phase 0.6 was a development-only diagnostic program. Its final D4-B result fell into the preregistered ambiguous state and the frozen route was STOP.

Frozen registry classification:

```text
D4-B AMBIGUOUS -> STOP
```

The reserved confirmatory seed namespaces remain outside the executed historical line.

## Post-Phase-0.6 analysis

The optimization-conditioned analysis after Phase 0.6 is registered separately as exploratory evidence. Its screening label is:

```text
structured optimization-conditioned heterogeneity worth prospective testing
```

It does not alter, rescue, or supersede the Phase 0.6 terminal decision.

## Phase 0.7 boundary

Phase 0.7 remains **design only** at this reproducibility boundary. It is not authorized for execution by R1, and nothing in the post-Phase-0.6 exploratory archive is treated as confirmatory evidence for it.

The reproducibility sequence is therefore:

```text
historical Phase 0
  -> historical Phase 0.5
  -> historical Phase 0.6 / D4-B AMBIGUOUS -> STOP
  -> explicitly exploratory post-Phase-0.6 analysis
  -> Phase 0.7 design only (not authorized for execution)
```
